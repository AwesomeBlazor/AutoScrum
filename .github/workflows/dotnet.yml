name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
        include-prerelease: true
    - name: Install Playwright
      shell: pwsh
      run: .\src\AutoScrum.UITests\bin\Debug\net6.0\playwright.ps1 install
    - name: Restore dependencies
      run: dotnet restore ./src/AutoScrum.sln
    - name: Build
      run: dotnet build --no-restore ./src/AutoScrum.sln
    - name: Test
      run: |
        SET BaseUrl=https://autoscrum.jkdev.me
        SET IsGitHubActions=true
        dotnet test --no-build --verbosity normal ./src/AutoScrum.sln
